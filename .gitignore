__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so

# Jupyter
.ipynb_checkpoints/

# Virtual environments
venv/
env/
.env

# Data files (too large for git)
data/raw/
data/processed/
*.wav
*.flac
*.mp3

# Model checkpoints (use git-lfs or cloud storage)
results/checkpoints/
*.pt
*.pth
*.ckpt

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# Dataset directory (stored on SSD via symlink — never commit)
data/
context.md

# Transferred dataset folder (full audio dataset — gigabytes, never commit)
dataset/
